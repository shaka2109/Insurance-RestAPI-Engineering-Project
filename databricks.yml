bundle:
  name: Insurance-RESTAPI

include:
  - resources/*.yml

targets:
  dev:
    default: true
    workspace:
      host: ${env.DATABRICKS_HOST}
    presets:
      name_prefix: "[DEV] "

  prod:
    workspace:
      host: ${env.DATABRICKS_HOST}
    presets:
      name_prefix: "[PROD] "

